# Working state produced by the ingest scripts. The published archive lives on
# the `archive` branch and in R2, never on master; aptly's database lives on the
# `aptly` branch.
/public/
/build/
/aptly-state/
/seed/
